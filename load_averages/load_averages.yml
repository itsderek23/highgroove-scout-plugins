options:
  # specify each option here
  max_load:
    # anyway to make all of these specifications optional, and just allow 'max_load' ?
    name: Max Load
    notes: If the server load is larger than this amount, an alert is generated
    default: 7
# these options are for rendering the visual graph on the rails app ... 
# these aren't in stone, still working on them
visualization:
  type: Line
  series: last_minute